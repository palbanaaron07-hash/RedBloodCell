name: Build and Deploy via FTP

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Build & Upload to Server
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Build Web App
        run: npm run build

      - name: Copy Server Config and PHP API
        run: |
          if [ -f ".htaccess" ]; then
            cp .htaccess dist/
          fi
          if [ -f "default.php" ]; then
            cp default.php dist/
          fi
          if [ -f "index.php" ]; then
            cp index.php dist/
          fi
          if [ -d "api" ]; then
            cp -r api dist/
          fi

      - name: DEBUG - List FTP directory structure
        run: |
          echo "=== ROOT DIRECTORY ==="
          curl -s --list-only "ftp://${{ secrets.FTP_SERVER }}/" --user "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" || echo "(failed to list root)"
          echo ""
          echo "=== public_html/ ==="
          curl -s --list-only "ftp://${{ secrets.FTP_SERVER }}/public_html/" --user "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" || echo "(no public_html/)"
          echo ""
          echo "=== public_html/bloodconnect/ ==="
          curl -s --list-only "ftp://${{ secrets.FTP_SERVER }}/public_html/bloodconnect/" --user "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" || echo "(no public_html/bloodconnect/)"
          echo ""
          echo "=== domains/ ==="
          curl -s --list-only "ftp://${{ secrets.FTP_SERVER }}/domains/" --user "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" || echo "(no domains/)"

      - name: Deploy to FTP Server
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: ${{ secrets.FTP_SERVER_DIR || './' }}
          local-dir: ./dist/
          dangerous-clean-slate: false
